// PostCSS config for bolt.diy
// bolt.diy uses UnoCSS (not Tailwind), so we don't need Tailwind PostCSS plugin
export default {
  plugins: {
    // Empty plugins - bolt.diy handles CSS through Vite and UnoCSS
  },
};
